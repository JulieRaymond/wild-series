    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Wild Series</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Catégories
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            {{ render(controller('App\\Controller\\CategoryController::showCategoryLinks')) }}
                        </ul>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownActors" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Acteurs
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdownActors">
                            {{ render(controller('App\\Controller\\ActorController::showActorLinks')) }}
                        </ul>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link favorite" href="{{ path('program_index') }}">Programmes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link favorite" href="#">Favoris</a>
                    </li>
                    {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link favorite" href="{{ path('app_profile') }}">Mon profil</a>
                    </li>
                    {% endif %}
                    <li class="nav-item">
                        {% if app.user %}
                            {{ app.user.email }}
                            <a class="nav-link" href="{{ path('app_logout') }}"> Se déconnecter</a>
                        {% else %}
                            <a class="nav-link" href="{{ path('app_login') }}"> Se connecter</a>
                        {% endif %}
                    </li>
                </ul>
                <!-- Espace pour le rond contenant la photo de profil -->
                <div class="profile-picture-container">
                    <!-- Insérez ici le code pour afficher la photo de profil, par exemple : -->
                    <!-- <img src="chemin/vers/photo-profil.jpg" alt="Photo de profil"> -->
                </div>
            </div>
        </div>
    </nav>
